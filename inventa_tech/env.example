# Copia este archivo como ".env" y coloca tus valores reales.
# El archivo ".env" NUNCA debe subirse al repositorio (agrégalo a .gitignore).

DB_HOST=db
DB_PORT=5432
DB_NAME=inventa_tech
DB_USER=postgres
DB_PASSWORD=coloca_aqui_tu_password_real
